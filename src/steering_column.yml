connectors:
  X-STEERING-1:
    <<: *x-dt06-12s
    pinlabels: [
      TURN SIGNAL SW,
      HEAD LT SW,
      HBEAM LT SW,
      CRUISE SPEED SW,
      WIPER SW,
      # UNUSED,
      # UNUSED,
      # UNUSED,
      # UNUSED,
      # UNUSED,
      # UNUSED,
      # GND,
    ]

cables:
  C-STEERING-1:
    colors: [
      GN, # TURN SIGNAL SW,
      RDWH, # HEAD LT SW,
      RDBU, # HBEAM LT SW,
      BK, # CRUISE SPEED SW,
      GNWH, # WIPER SW,
    ]
    wirelabels: [
      TURN SIGNAL SW,
      HEAD LT SW,
      HBEAM LT SW,
      CRUISE SPEED SW,
      WIPER SW,
    ]

connections:
  - - X-STEERING-1: [
        TURN SIGNAL SW,
        HEAD LT SW,
        HBEAM LT SW,
        CRUISE SPEED SW,
        WIPER SW,
      ]
    - C-STEERING-1: [
        TURN SIGNAL SW,
        HEAD LT SW,
        HBEAM LT SW,
        CRUISE SPEED SW,
        WIPER SW,
      ]
    - X-PMU: [
        INPUT A2,
        INPUT A4,
        INPUT A6,
        INPUT A8,
        INPUT A13,
      ]
